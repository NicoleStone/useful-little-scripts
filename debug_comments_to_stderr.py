#!/usr/bin/env python3

GLOBAL_DEBUG = True # make this True if you want debugging comments printed to terminal, False if not
def debugPrint(msg):
	if GLOBAL_DEBUG:
		print(msg)
		
debugPrint("hello")
